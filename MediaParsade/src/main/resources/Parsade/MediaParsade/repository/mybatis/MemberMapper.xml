<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Parsade.MediaParsade.repository.mybatis.MemberMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member (name, studentId)
        SELECT #{name}, #{studentId}
        FROM DUAL
        WHERE NOT EXISTS (
        SELECT 1
        FROM member
        WHERE studentId = #{studentId}
        )
    </insert>

    <update id="update">
        update member
        <set>
            <if test="updateParam.type == 'function'">
                type = #{updateParam.type},
                text = #{updateParam.text},
                property = #{updateParam.property}
            </if>
            <if test="updateParam.type == 'common'">
                type = #{updateParam.type},
                text = #{updateParam.text},
                selection = #{updateParam.selection}
            </if>
        </set>
        where id = #{id}
    </update>


    <select id="findAll" resultType="ReturnMember">
        select name, studentId, type, text, selection, property
        from member
    </select>


</mapper>